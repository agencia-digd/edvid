# AGENTS.md — Codex / Claude Code

Este workspace É a skill Edvid (fork dig.D).

1. Leia `CLAUDE.md` e `SKILL.md` antes de qualquer corte.
2. Fase 2 (Remotion): leia `skills/remotion-best-practices/SKILL.md`.
3. Instale com `uv sync` neste diretório. Rode helpers via `uv run python helpers/<nome>.py`.
4. Sem ffmpeg/node no PATH: instale no Linux da sessão (`apt`/`brew`) e siga.
5. Não comece a editar porque um vídeo chegou. Espere ela nomear o arquivo e pedir.
